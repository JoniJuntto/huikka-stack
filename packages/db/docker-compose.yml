name: huikka-stack

services:
  postgres:
    image: postgres
    container_name: huikka-stack-postgres
    environment:
      POSTGRES_DB: huikka-stack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - huikka-stack_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  huikka-stack_postgres_data:
